"""Thin uvx shim -- delegates to install.sh so `uvx --from
git+https://github.com/jthiruveedula/agentkit agentkit` works with zero
build step. The real installer logic lives in install.sh, not here.
"""
